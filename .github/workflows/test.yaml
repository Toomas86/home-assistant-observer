name: Test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: Install test dependencies
        run: python -m pip install PyYAML==6.0.2 ruff==0.12.9
      - name: Unit tests
        run: python -m unittest discover -s tests -v
      - name: Lint Python
        run: ruff check ha_observer/rootfs/app/observer tests
      - name: Compile Python
        run: python -m compileall -q ha_observer/rootfs/app/observer tests
      - name: Check shell script
        run: shellcheck ha_observer/run.sh
